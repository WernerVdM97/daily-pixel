## RESOLVE RECIPE — TRAVEL SUCCESS

**Successful journey:**
- Always: `move_to` — name the destination
- Always: `modify_stamina` -1 to -2 (the journey)
- Often: `add_npc` (someone you meet) OR `add_item` (1-2 items found along the way)
- When pushing into unmarked territory: `reveal_location` (name the road they glimpsed)
- Apply Rule 2a: on a natural 20, double the discovery — two items, a memorable NPC, or two `reveal_location` glimpses.
