---
title: JSON Seam — Milestone Build Plans (M0–M4)
status: decided
domain: engine
phase: mvp
tags: [architecture, layering, engine, controller, json, seam, build-plan]
related:
  - "[[layer-boundaries-and-json-seam]]"
---
Task-level build plans for the milestones in [[layer-boundaries-and-json-seam]] (the running spec; its Decisions section is binding). One section per milestone, written by the implementing lead just before that milestone starts. The parent doc keeps the milestone checkboxes; this doc holds the per-milestone task breakdown, execution state, and commit hashes.

---

## M0 — Commute rule into the engine

Goal: the engine owns the day-job commute rule ("standing at The Warden's Oak with a workplace elsewhere → move there, −1 stamina floored at 0, record the fog-of-war visit"); the Discord handler only calls the engine and renders the result. Deletes the Discord layer's sole direct `charRepo.update` (`src/index.ts`, the `── Commute from the Oak to the workplace ──` block).

Design calls (lead, within the parent doc's decision 4):

[I] The rule lands as a dedicated engine method `commuteToWorkplace(characterId, workplace)` called by the handler right where the old block sat, rather than buried inside `startAction`. Rationale: byte-for-byte Discord UX (the transient "🚶 Daily Commute" embed must render *before* the seconds-long LLM call, so the handler needs the commute result up front), and decision 4's "one site, engine-testable in isolation". Folding it into the `startAction` work path can ride M3 when the controller owns the flow.
[I] Workplace *resolution* (`getWorkplaceLocation`) stays caller-side for M0: it shares a seeded PRNG with `getDayJobActions` in `src/discord/commands/hi.ts` so `/hi` and the action agree within a day; splitting that pair across layers is out of M0's one-site scope. The engine receives the resolved destination as input and owns every other part of the rule (the at-Oak condition included).
[I] `WorldEngine.recordVisit` exists only for this commute block (sole caller `src/index.ts:2068`); the new method absorbs the visit recording, so `recordVisit` is removed from the interface and impl.

Tasks:

[x] Engine: add `commuteToWorkplace(characterId: number, workplace: string | null): { to: string; stamina: number } | null` to `WorldEngine` + `WorldEngineImpl` (row via `charRepo.findById`; condition `location === "The Warden's Oak" && workplace && workplace !== location`; `Math.max(0, stamina − 1)`; one `charRepo.update`; `charLocRepo.recordVisit`; null when no commute applies).
[x] Engine: remove the now-callerless `recordVisit` from `WorldEngine` + `WorldEngineImpl` (also re-stubbed in `MockWorldEngine`, a necessary follow-on).
[x] Discord: replace the commute block in `src/index.ts` with the engine call; patch the local `char` snapshot from the result (preserves the `announceCollapse` before-baseline); keep the transient embed text byte-identical.
[x] Tests: new `tests/engine/commute.test.ts` mirroring `visit-recording.test.ts` setup — happy path (move + stamina −1 + visit recorded + persisted), stamina-0 floor, not-at-Oak no-op, null workplace no-op, workplace-equals-location no-op.
[x] Lead closeout: verify, commit, review loop, changelog entry, tick M0 in the parent doc.

Execution state: _done 2026-07-18._ Build commit `98a4de1` (typecheck clean; 78 files / 1462 tests green, +5 over baseline). Adversarial review found no code defects; two informational notes recorded: the engine re-reads the row at call time (fresher than the old handler snapshot under any future concurrency — deliberate), and the handler-level e2e gap is M1's job, not M0's.

## M1 — Behavioural oracle

Goal: a characterisation baseline — golden transcripts of the `index.ts` dispatcher's per-screen behaviour, diffable after every extraction step. Hard prerequisite for M3: it is the safety net the branch extraction migrates against, so it must observe exactly the branches M3 will move.

Groundwork (surveyed 2026-07-18, post-M0; line numbers predate the M1.1 hoist and shift once the closure moves):

[I] The dispatcher is a single closure `dispatchInteraction` inside `main()` (`src/index.ts:1487-2417`), registered at `client.on(Events.InteractionCreate, …)` (`:2419-2437`) behind the `_interactionInFlight` guard (`:362`, key via `interactionGuardKey` `:364`). It is not exported, and `src/index.ts` self-executes on import (DB open, YAML assets, LLM gateway/env, `client.login`), so no test can import it today.
[I] Branch inventory: one slash-command arm (`:1489`; character-gate reroute; hard-coded ephemeral list; sleep feedback-row append) plus 14 customId branches, of which `nav:` holds 3 sub-branches (`nav:action` `:2204` re-implements the /action menu, `nav:sleep` `:2330`, generic nav `:2372`) — 17 leaf behaviours total. The heaviest and least-tested is the `action:dayjob:` work flow (`:2000-2165`).
[I] Reachable from tests today (pattern in `tests/discord/`): exported command factories (`makeXCommand` + `MockWorldEngine` + hand-rolled fake interactions with `vi.fn()` spies — see `nav-action.test.ts:17`, `join.test.ts:22`), the exported `handleJoinInteraction`, and the pure builders. Locked inside the closure and unreachable: the whole `nav:` bar, the `action:dayjob:` work flow, the `action:custom:modal` chain, the four feedback/bug button→modal chains, and the slash-arm assembly (gate/ephemeral/payload) — plus the customId cascade order itself.
[I] Module-level flow state, keyed by userId, with NO exported clear-all: `pendingDecisions` + `_menuMessages` + `_sceneLookup` (`src/discord/commands/action.ts:90-107`), `_userInFlight` (`join.ts:63`). Wizard `sessions` (`WizardSession.ts:36`) is instance-scoped (fresh on `new WizardSession()`). `_interactionInFlight` (`index.ts:362`) lives at the registration site, outside the closure.
[I] No snapshot testing exists anywhere in `tests/` yet — the golden-transcript harness is greenfield; vitest snapshots are available.

Design point — SETTLED 2026-07-18 (resolves the `[!]` the parent doc's decision 4 left to the lead): **minimal DI hoist, not factories-only.** M1 is the safety net for M3, and M3's whole job is extracting these dispatcher branches — including the six areas locked inside the closure (the `nav:` bar, `action:dayjob:` work flow, `action:custom:modal`, the feedback/bug chains, the slash-arm assembly) and the cascade order. A factories-only oracle sees none of them, so it cannot protect the extraction; it is disqualified by purpose. The hoist is scaffolding for M1, explicitly NOT the M3 controller extraction — the closure body moves verbatim and the branches stay a byte-identical `if/else` on customId; M3 later reshapes them into a transport-neutral controller.

Design calls flowing from the survey:

[I] Guard + error funnel live OUTSIDE the closure, at the `client.on(InteractionCreate)` registration site (`:2419-2437`), not inside `dispatchInteraction`. So the hoist is a pure move of the closure body; the guard/funnel wrapper stays in `main()` and calls the hoisted function. M1's oracle characterises the closure's 17 leaf behaviours + cascade order (the M3 safety net); the registration-site guard/funnel is [[discord-interaction-layer]] plumbing (decision 5, sequenced after extraction) and is out of M1's golden-transcript scope — at most one cheap guard test if it falls out of the same harness for free.

[I] Byte-identical-body invariant. The hoist injects a `DispatchDeps` object and destructures it into locals at the function top under the SAME names the closure captured, so the ~930-line body below is copied character-for-character. If the body is verbatim, behaviour cannot change — this is the invariant the reviewer verifies, and it makes the "a snapshot cements a hoist bug" risk moot (there is nothing to cement).

[I] DI surface. Everything the closure references that is constructed in `main()`'s scope OR is module-level in the self-executing `src/index.ts` becomes an injected dep, so the new module never imports from `index.ts` and stays free of its DB-open/`client.login` self-exec. Compiler-guided: after the move, every unresolved symbol is either a Group-B import from a non-self-executing module (add the import) or a dep (add to `DispatchDeps`). Known members from the survey: `engine`, `registry`, `getCurrentScene`, `dayJobs`, `joinWizards`, `notifyAdmin`, `safeErrorReply`, `VERBOSE`, `ADMIN_USER_ID`, `CHARACTER_GATED_COMMANDS` — final list is whatever the compiler surfaces. Definitions are NOT relocated; `index.ts` stays their owner (relocation is M3's remit).

[I] Snapshot determinism. Neutralise nondeterminism before snapshotting — `randomIdleMessage` (`IdleMessageSelector`), any `Date`/now in footers, random art/banner selection: mock or seed so transcripts are stable. Because the four action/join maps have no clear-all, key every transcript on a UNIQUE userId to avoid cross-transcript bleed; construct a fresh `WizardSession` per transcript.

Tasks:

M1.1 — Hoist (own commit, mechanical refactor):
[x] Create `src/discord/dispatchInteraction.ts` exporting `async function dispatchInteraction(interaction: Interaction, deps: DispatchDeps): Promise<void>` and the `DispatchDeps` interface; move the closure body from `src/index.ts:1487-2417` verbatim, destructuring `deps` into same-named locals at the top.
[x] In `src/index.ts` `main()`, import and call it; the `client.on` guard/funnel wrapper (`:2419-2437`) stays, now wrapping `dispatchInteraction(interaction, deps)`, with `deps` wired from the existing main()-scope bindings.
[x] Verify: `npm run typecheck` clean; `npm test` green at 78 files / 1462 tests (no delta — pure move, zero behaviour change).

M1.2 — Oracle (own commit, tests):
[x] Golden-transcript harness under `tests/discord/` mirroring the existing fake-interaction pattern (`MockWorldEngine` + fresh `WizardSession` + `vi.fn()` spy interactions; assert on `spy.mock.calls[n][0]` embeds/components/modal shape), using vitest `toMatchSnapshot`.
[x] Cover all 17 leaf behaviours: the slash arm (gate/reroute/nav-buttons), the 14 customId branches, and the 3 `nav:` sub-branches — including the heaviest, the `action:dayjob:` work flow.
[x] Pin cascade ORDER via the specific-before-broad cases: `action:dayjob:custom` before `action:dayjob:`, `action:custom:modal` before `action:`, and `action:dayjob:` before `action:`.
[x] Unique userId per transcript; neutralise the nondeterminism sources above; snapshots committed.
[x] Verify: `npm test` green with the new suite; every transcript produces stable, meaningful output (not empty spies).
[x] Review fix: characterise the resolved/outcome render path (was uncovered — every transcript stopped at a decision) for the three action leaves; `broadcastOutcome`/`announceCollapse` neutralised via asserted spies; `getCurrentScene` given a fixed realistic scene.

Scope fence (both tasks): no branch logic changes; no definition relocation; do not touch `action.ts` / `join.ts` / `WizardSession.ts` internals (import them as-is); no engine changes; M2/M3 shaping is out of bounds.

Execution state: _done 2026-07-19._ M1.1 hoist `b9b4c4a` (DI surface = the 10 expected `DispatchDeps` fields; body verbatim, reviewer-confirmed 0-diff over 929 lines). M1.2 oracle `2a3a069` + review fix `0bd0401` (all 17 leaves + the resolved/outcome sub-path; cascade order pinned with branch-fired assertions). Final: typecheck clean, 79 files / 1489 tests green (+27 over the M0 baseline). Known deliberate property (accepted review finding, not a defect): the day-job snapshots bake in real `assets/char-creation/day-jobs.yml` content via the seeded sampler — a future YAML edit will (correctly) churn those snapshots; keep the driven day-job indices within Town Guard's real action pool.

## M2 — Semantic view-state DTO + shared renderers

Goal: introduce the semantic view-state DTO (parent doc decision 2) and the view-state→medium step, then port `buildDecisionMessage` / `buildOutcomeEmbed` (both in `src/discord/commands/action.ts`) behind them so Discord output is **byte-identical**. This is a pure internal refactor: the two public builders keep their exact signatures and output; no call site changes. The M1 oracle (`tests/discord/dispatch-oracle.test.ts`), `tests/discord/action-decision.test.ts` and `tests/discord/action-divine.test.ts` are the byte-identical gate — all must stay green with zero snapshot churn.

Design calls (lead, within decision 2):

[I] The seam splits each builder in two. `buildXView(...)` assembles a **semantic** `ViewState` DTO — named text/ANSI content blocks (all produced by the existing neutral string helpers) plus option semantics, footer, title and a colour *intent* — importing nothing from `discord.js`. `XViewToDiscord(view)` is the **medium step**: it owns block joining, the embed-length degradation ladder, and all `EmbedBuilder` / `ButtonBuilder` / `ActionRowBuilder` construction. The public builder becomes `XViewToDiscord(buildXView(...))`.

[I] The embed 4096-char degradation ladder is a *medium* concern (a Discord embed cap; a web adapter would not have it), so it lives in the medium step. To keep it byte-identical the DTO carries BOTH the full and the collapsed story-thread variant (each is just `buildStoryThread(..., collapse, ...)` with a fixed boolean), so the medium step re-runs the exact same `assemble`/degrade decisions on pre-rendered strings.

[I] Colour is carried as a semantic `ViewColorIntent` union; the medium step maps intent→hex, reproducing `outcomeColor()` and the decision constant `0xdaa520` exactly. The opening-frame embed's fixed `0x2c2f33` stays internal to the medium step (it is medium chrome, never a semantic choice).

[I] File layout. DTO types → new `src/view/viewState.ts` (a module that imports NOTHING from `discord.js` — that non-import is the structural guarantee the DTO is transport-neutral). Medium step → new `src/discord/viewToDiscord.ts` (owns the `discord.js` weld; produces embed/component JSON, not strings, so it belongs in the adapter layer, not the pure `src/render/*`). The text helpers (`quoteLines`, `clip`, `dcArrow`, `statEmoji`, `buildStoryThread`, `renderCombatStatusFrame`, `MAX_EMBED_DESC`, `INSIGHT_MARGIN`) stay in `action.ts` and are imported/used as-is; relocation is M3's remit (mirrors M0/M1 "definitions not relocated").

DTO shape (final — the executor implements exactly this):

[I] `DecisionViewState` = `{ screen: 'decision'; title: { emoji; text }; colorIntent: 'decision'; storyThread?: { full: string; collapsed: string }; narration?: string; combatStatus?: string; prompt: string; optionLines: string[]; buttons: Array<{ kind: 'choice'; letter: string; customId: string; favoured: boolean } | { kind: 'bail'; label: string; customId: string }>; footer: string; openingFrame?: string }`. `narration`/`combatStatus`/`prompt`/`storyThread.*`/`optionLines`/`openingFrame` are the already-rendered strings the current code produces (quoted narration/prompt, ANSI frames, `**A.** …` option lines); `buttons` mirror the current `ButtonBuilder` set 1:1 (bail carries `shortLabel(opt.label, 80)`; choice carries just the letter).

[I] `OutcomeViewState` = `{ screen: 'outcome'; title: { emoji; text }; colorIntent: ViewColorIntent; locationLine?: string; breadcrumb?: string; sceneBlock?: string; combatSceneBlock?: string; isCombat: boolean; storyThread?: { full: string; collapsed: string }; outcomeBlock: string }`. `storyThread` is absent when `opts.compact` (the current `!compact` guard); `isCombat` = `!!outcome.combatBeat` (selects `combatSceneBlock` over `sceneBlock` when including the scene). The medium step's `assemble(collapseHistory, includeScene)` reproduces the current part order exactly: `locationLine`, `breadcrumb`, scene (combat vs plain per `isCombat`+`includeScene`), story-thread (unless compact), `outcomeBlock`.

[I] `ViewColorIntent` = `'decision' | 'success' | 'failure' | 'skipped' | 'bailed' | 'done' | 'timed_out' | 'default'`. `buildOutcomeView` maps `outcome.outcome` to the intent; the medium step maps intent→hex per the current `outcomeColor` switch.

Tasks:

M2.1 — DTO + medium step + port (own commit):
[x] Create `src/view/viewState.ts`: export `DecisionViewState`, `OutcomeViewState`, `ViewState` (union), `ViewColorIntent`, and the button-item union type. No runtime code, no `discord.js` import.
[x] Create `src/discord/viewToDiscord.ts`: export `decisionViewToDiscord(view: DecisionViewState): { embeds; components }` and `outcomeViewToDiscord(view: OutcomeViewState): ReturnType<EmbedBuilder['toJSON']>`. Move the join + degradation ladder + `EmbedBuilder`/`ButtonBuilder`/`ActionRowBuilder`/5-per-row chunking + colour-intent→hex mapping here, verbatim in behaviour.
[x] In `action.ts`: add `buildDecisionView(...)` and `buildOutcomeView(...)` (same parameter lists as the current builders) returning the DTOs; re-express `buildDecisionMessage` as `decisionViewToDiscord(buildDecisionView(...))` and `buildOutcomeEmbed` as `outcomeViewToDiscord(buildOutcomeView(...))`. Export `buildDecisionView`/`buildOutcomeView`. Keep every helper in place.
[x] Verify: `npm run typecheck` clean; `npm test` green at 79 files / 1489 tests with ZERO snapshot churn (`git diff --stat` shows no change under `tests/discord/__snapshots__/`). If any snapshot changes, the port drifted — fix the port, never re-bless the snapshot.

M2.2 — DTO-level test (own commit, or fold into M2.1 at lead's call):
[x] Add `tests/discord/view-state.test.ts` asserting `buildDecisionView`/`buildOutcomeView` return the expected semantic shape for a representative decision (with options + favoured hint) and outcome (combat + non-combat), plus direct medium-step tests exercising `decisionViewToDiscord`/`outcomeViewToDiscord` on hand-built DTOs (embed/button assembly, the degradation ladder, colour-intent mapping incl. the default fallback). This pins the seam independently of the Discord snapshots.

Scope fence: no behaviour/branch changes; no caller changes in `dispatchInteraction.ts` or `action.ts` button handlers; do not relocate helper definitions; no engine changes; no `src/render/*` changes; M3 controller shaping is out of bounds. The DTO is a presentation view-state (screen/prompt/options/art/footer), NOT an engine or protocol type.

Execution state: _done 2026-07-19._ Build `2275a42` (DTO + medium step + port; typecheck clean, 80 files / 1500 tests green, +11 over the M1 baseline, ZERO snapshot churn — Discord output byte-identical, confirmed by the untouched M1 oracle + action snapshots). Review fix `a04ede2` (the M2.2 round-trip assertions were tautological — `buildXMessage` *is* `viewToDiscord(buildXView(...))` — so replaced with direct hand-built-DTO medium-step tests that pin the join, the degradation ladder, and the colour mapping independently; 1502 tests green). Adversarial review found no byte-drift across a line-by-line diff of both builder pairs and an exhaustive colour-mapping check. Deferred to M3 (informational, non-blocking): `viewToDiscord.ts` ↔ `action.ts` is a two-file import cycle (the medium step pulls `clip`/`MAX_EMBED_DESC`/`outcomeColor` back from `action.ts`) — safe today (all refs are in function bodies; no init-order hazard) and sanctioned by the "helpers stay in `action.ts`" design call; M3 relocates those helpers to a neutral util and the cycle dissolves.

## M3 — Controller extraction, screen-by-screen

Goal: a transport-neutral session controller (`src/controller/*`, never imports `discord.js`) absorbs the flow smeared across `dispatchInteraction.ts` and the command-file resume/decision paths; the engine absorbs option-resolution (parent decision 1) so the `pendingDecisions` map in `action.ts` dies; the thin Discord adapter is left with transport + paint only. The M1 oracle (`tests/discord/dispatch-oracle.test.ts`) plus the M2 action snapshots are the byte-identical gate — green after **every** slice, zero snapshot churn, or the extraction drifted.

### Sizing task 1 (done) — dispatcher branch inventory

All 17 `dispatchInteraction` leaves bucketed. The recurring shape is a sandwich: a Discord ack (`deferReply`/`showModal`/`reply`) wraps a game-flow core wraps a Discord paint (`editReply`/`webhook.editMessage`). Extraction cuts the filling out of the bread.

[I] **Bucket A — pure-Discord (stays in the thin adapter):** the transport verbs (`deferReply`/`deferUpdate`/`showModal`/`reply`/`editReply`/`webhook.editMessage`/`fetchReply`); all 5 `ModalBuilder` constructions; payload assembly (`buildComponentPayload`, `getNavButtons`, `navResponseMode`, banner/`imageFiles`, the `ephemeral` command-list policy); the `WebhookClient(...).deleteMessage` mechanics; the error funnel (`notifyAdmin` + `safeErrorReply`) — the funnel is [[discord-interaction-layer]] plumbing (parent decision 5, sequenced *after* extraction), so it stays wrapping the adapter and M3 does not touch it.
[I] **Bucket B — game-flow (moves to the controller):** the character-gate reroute; `updateLastPlayed` stamping; the resume-vs-start-vs-decision core duplicated across four leaves (`action:custom:modal`, `action:dayjob:`, `nav:action`, `action:`→`handleActionChoice`); the at-workplace / safe-ground **work-eligibility rule** (a game rule in the UI — same smell as the M0 commute leak); the rolls-remaining guard; `commuteToWorkplace` + `workPrompt` + `startAction({kind:'work',wage})`; day-job menu composition (`getDayJobActions` + `buildActionHints`); the compact-private / full-public outcome fan-out decision (`broadcastOutcome` + `announceCollapse`); feedback/bug submission routing; and the option-resolution state that dies (`pendingDecisions` + `getChoiceLabel` + `setPendingDecision`).
[I] **Bucket C — straddles the seam, resolved below:** the view-state vocabulary gap, the caller-side workplace-resolution PRNG pair, and the modal round-trip protocol.

### Settled design calls (2026-07-19)

[I] **Extend the view-state union (Bucket C.1).** M2 defined `DecisionViewState` + `OutcomeViewState` only, but the dispatcher hand-builds ~6 further inline embeds (thinking, 🚶 commute beat, day-job menu, "⏳ Stale Action", empty-decision "⚔️ Action", loading beats). M3 grows the `ViewState` union to cover them (semantic `MenuViewState`, `LoadingViewState`, `NoticeViewState`, `CommuteViewState` — final names the implementing slice's call) so the controller emits every screen and M4's agent-player sees what a Discord player sees. Each new view-state ships **in the slice that first needs it**, never as unused vocabulary ahead of a caller.
[I] **Move the day-job PRNG pair into the controller (Bucket C.2).** M0 deliberately left `getWorkplaceLocation` caller-side because it shares a seeded PRNG with `getDayJobActions` (`src/discord/commands/hi.ts`). M3 owns the day-job flow now, so the pair relocates **together** into the controller (splitting them across layers is what M0 refused, not the move itself); `/hi` keeps calling the relocated helpers so the day agrees within a day.
[I] **Modal round-trip protocol (Bucket C.3).** The controller stays ignorant of `discord.js` modals: it exposes an intent ("collect \<feedback|bug|custom-action\> text, carrying actionId X") and a submission entry that takes the returned string; the adapter owns `ModalBuilder`/`showModal` and hands the text back. This recurs 5× (feedback ×3, bug, custom action), so the protocol is pinned once in M3.1 and reused.

### Slice sequence

Order is binding; the oracle + M2 snapshots gate every slice. Detailed task checklists are written per-slice just before that slice starts (the plan sketches each slice's goal + the seam it establishes; over-specifying seven checklists upfront would only be rewritten as the controller shape settles).

[x] **M3.0 — Dissolve the M2 import cycle (mechanical, first).** _(done 2026-07-19, commit `825ae41`)_ Relocated `clip`, `MAX_EMBED_DESC`, `outcomeColor` verbatim out of `src/discord/commands/action.ts` into the neutral `src/render/embedText.ts` (imports nothing from `discord.js`); `viewToDiscord.ts` repointed to import **down** from it. The `viewToDiscord` ↔ `action` edge is now one-way — cycle dissolved. Typecheck clean, 80 files / 1502 tests green, zero snapshot churn (verbatim move confirmed by lead + fresh-context reviewer).
[x] **M3.1 — Controller skeleton + feedback/bug submissions.** _(done 2026-07-19, commit `1432d59`)_ Stood up `src/controller/SessionController.ts` (transport-neutral: imports only `WorldEngine` + view-state types, never `discord.js`) and routed the four modal-submit leaves through it (`feedbackConfirmation(surface)` returns a `NoticeViewState`; `recordFeedback(surface, userId, text, actionId?)` does the best-effort persist). Added `NoticeViewState` to `viewState.ts` + `noticeViewToDiscord` to the medium step; wired `controller` into `DispatchDeps` (both `index.ts` and the oracle harness). The modal-open leaves stayed pure adapter (Bucket A). Byte-identical: 81 files / 1512 tests green (+10 controller/medium-step unit tests), zero snapshot churn — confirmed by lead + fresh-context reviewer. Established the seam M3.2-M3.6 reuse: adapter extracts transport (text, parsed actionId) → controller returns a `ViewState` + performs engine side-effects → adapter paints via `*ViewToDiscord`; reply-first best-effort ordering preserved via the confirmation/record split.
      - Deferred follow-up (non-blocking, from M3.1 review): no regression test pins the reply-first resilience of these four leaves (a throwing `recordFeedback` must not suppress the confirmation reply). Structurally obvious today; add a dispatch-level throwing-persist test if this area is revisited.
[x] **M3.2 — Action choice + option-resolution death.** _(done 2026-07-19, commits `dc7b5a1` + `a9c30d1` + `a1363c7`)_ Absorbed `handleActionChoice` into the controller; the engine absorbs option-resolution via `resolvePendingChoice` reading `last_action_state.pendingDecision` (parent decision 1); the `pendingDecisions` map + `getChoiceLabel` + `setPendingDecision` are deleted. Reuses the M2 `Decision`/`OutcomeViewState`.
[x] **M3.3 — nav:action menu + day-job menu composition.** _(done 2026-07-19, commits `890c297` + `c04d2b0`)_ Relocated the day-job domain (PRNG pair + types + `buildActionHints` + `CID_DAYJOB*`) to `discord.js`-free `src/controller/dayJob.ts`; added `MenuViewState` + `menuViewToDiscord` + `composeActionMenu`; `SessionController.openActionMenu` owns the `nav:action` flow; both `nav:action` and the slash `/action` no-description path render the menu through the one composition. Byte-identical (nav:action oracle snapshot unchanged).

  M3.3 design calls + checklist (lead-settled 2026-07-19):
  [I] **Relocate the day-job domain module (DC-G).** Move `getDayJobActions`, `getWorkplaceLocation`, the private `mulberry32`/`COMMON_ACTIONS`/`WANDERER_SPOTS`, the `DayJobDef`/`DayJobAction` types, and `buildActionHints`/`ActionHintContext` (currently split across `src/discord/commands/hi.ts` + `action.ts`), plus the `CID_DAYJOB`/`CID_DAYJOB_CUSTOM` id constants, into a new `discord.js`-free `src/controller/dayJob.ts`. Verbatim move; update every importer (`hi.ts`, `sleep.ts`, `action.ts`, `dispatchInteraction.ts`, `index.ts`, tests); `/hi` and `/sleep` keep calling the relocated helpers so the seeded day agrees within a day (the whole point of C.2 — move the pair together, don't split it across layers). Zero behaviour change, zero snapshot churn.
  [I] **MenuViewState + single composition (DC-H).** Add `MenuViewState` to `src/view/viewState.ts` (`{ screen: 'menu'; title: { emoji; text }; description: string; buttons: Array<{ label: string; customId: string; style: 'secondary' | 'primary' }> }`) and `menuViewToDiscord` to the medium step (builds the embed `0xdaa520` + one `ActionRow` of `ButtonBuilder`s, style-intent -> `ButtonStyle`). The controller composes it once (`composeActionMenu(character): MenuViewState` using the relocated `getDayJobActions` + `buildActionHints` + `dayJobEmoji` + the `CID_DAYJOB*` ids); BOTH the `nav:action` leaf and the slash `/action` no-description path (`makeActionCommand`) render through it, killing the duplication that `buildActionHints`' shared-helper comment was guarding against.
  [I] **Controller owns the nav:action flow (DC-I).** Add `SessionController` constructor dep `dayJobs: DayJobDef[]`; add `openActionMenu(userId): ActionMenuResult` = `{ kind: 'no-character' } | { kind: 'no-rolls' } | { kind: 'resume-stale'; prompt: string; narration?: string } | { kind: 'resume-decision'; view: DecisionViewState } | { kind: 'resume-error'; message: string } | { kind: 'menu'; view: MenuViewState }` reproducing the leaf's order exactly (char guard -> rolls guard `rollsRemaining <= 0 && !lastActionState` -> if `lastActionState`: `resumeAction` in a try, empty-options -> `resume-stale`, else `resume-decision` via `buildDecisionView`, throw -> `resume-error` -> else `menu`). The adapter paints each branch (the "⏳ Stale Action" embed stays an inline adapter embed — not yet a view-state; unifying it is a later slice) and still owns the `fetchReply` + `stashMenuMessage` mechanics + the `updateLastPlayed` stamp (Bucket A). Byte-identical: oracle `nav:action` transcript + any `/action` menu test stay green, zero churn.
  M3.3a — Relocate day-job module (own commit, verbatim per DC-G).
  M3.3b — MenuViewState + composition + nav:action + slash-menu wiring (own commit per DC-H/DC-I).
  Scope fence: do NOT port `action:dayjob:` (M3.4) or `action:custom:modal` (M3.5); `navResponseMode`/`buildComponentPayload`/`ephemeral` policy stay adapter; no engine changes beyond what already exists; every commit byte-identical.
[x] **M3.4 — Day-job work flow.** _(done 2026-07-19, commit `f211a4e`)_ Ported `action:dayjob:` (the heaviest leaf) through `SessionController.beginDayJob`/`commuteForWork`/`runWork`; added `LoadingViewState` + `CommuteViewState`. The old local-char patch became a post-commute `getCharacter` re-read (faithful to the engine's persist; `MockWorldEngine` updated to mirror it). Fresh-context review verified byte-identity against the REAL engine (not just the doctored mock) + the compact/full fan-out; zero findings. Byte-identical, zero churn.

  M3.4 design calls + checklist (lead-settled 2026-07-19). Single commit (view-states are additive + only this slice uses them). The leaf's shape is: guards -> [transport: defer + "Starting…" loading] -> commute -> [transport: "Daily Commute" beat] -> `startAction({kind:'work',wage})` -> [transport: outcome/decision paint + broadcast]. Transport paints interleave between game steps, so the controller exposes THREE staged methods and the adapter paints between them; the whole leaf stays inside ONE outer `try` (the leaf has no inner catch today — any throw -> `notifyAdmin` + "Could not act").
  [I] **View-states (DC-J).** Add `LoadingViewState = { screen: 'loading'; body: string }` (medium: `EmbedBuilder().setDescription(body).setColor(0x95a5a6)`, `components: []`) and `CommuteViewState = { screen: 'commute'; destination: string; idle: string }` (medium: title `🚶 Daily Commute`, description `**You head to the ${destination}.**  \n⚡ -1 stamina\n\n⏳ **Setting to work…**\n_${idle}_`, grey, `components: []`) to `viewState.ts` + `viewToDiscord.ts`. The adapter calls `randomIdleMessage()` ONCE and threads that single value into the "Starting…" loading body AND the commute `idle` (matching the current `idleMsg` reuse) so idle-message call count is unchanged.
  [I] **Controller staged methods (DC-K).** `beginDayJob(userId, idx): { kind: 'no-character' } | { kind: 'invalid-job' } | { kind: 'unsafe'; location: string } | { kind: 'ok'; workplace: string | null; workPrompt: string; wage: number }` reproduces the guard order EXACTLY: getCharacter null -> no-character; `updateLastPlayed`; `getDayJobActions[idx]` -> `jobAction`, `!jobAction?.hook` -> invalid-job; `getWorkplaceLocation`; `atWorkplace = workplace !== null && char.location === workplace`; `!getLocation(char.location)?.isSafe && !atWorkplace` -> unsafe (carry `char.location`); else ok with `workPrompt = \`${jobAction.label} — ${hook}\`` and `wage = jobAction.income ?? 0`. `commuteForWork(userId, workplace): { kind: 'commuted'; destination: string } | { kind: 'none' }` wraps `engine.commuteToWorkplace(char.id, workplace)` (which persists stamina/location). `runWork(userId, workPrompt, wage): { kind: 'outcome'; viewPrivate: OutcomeViewState; viewPublic: OutcomeViewState; distilledType; actionId?; characterName; char; prevChar } | { kind: 'empty-action'; prompt: string } | { kind: 'decision'; view: DecisionViewState }` re-reads `prevChar = getCharacter(userId)` (post-commute), `startAction(prevChar.id, workPrompt, { kind: 'work', wage })`, then: `outcome` -> re-read `char = getCharacter(userId) ?? prevChar`, build `viewPrivate` (compact) + `viewPublic` (full) via `buildOutcomeView(..., getCurrentScene(userId), state, opts, engine)`, `characterName = char.name` (the dayjob public content line has NO `classEmoji` — just `**name**`); `firstDecision.options.length === 0` -> empty-action; else `decision` via `buildDecisionView(firstDecision, 0, state, prevChar, actionType)`. `runWork` does NOT catch (errors propagate to the adapter's single outer try, matching today).
  [I] **Adapter (DC-L).** Rewrite the `action:dayjob:` leaf inside its existing outer `try/catch (notifyAdmin "Action (day-job) failed" + "Could not act" webhook.editMessage)`: `beginDayJob` -> no-character/invalid-job/unsafe reply inline (ephemeral); else `deferUpdate` + paint the "Starting…" `LoadingViewState` (idle threaded); `commuteForWork` -> if commuted, paint the `CommuteViewState`; `runWork` -> outcome paints `webhook.editMessage(outcomeViewToDiscord(viewPrivate))` + nav/service buttons, then the public `payload` (`**${characterName}** <@id> — ${distilledType}`, `outcomeViewToDiscord(viewPublic)`, public buttons), `broadcastOutcome`, `announceCollapse(char.name, prevChar, char)`; empty-action paints the inline "⚔️ Action" grey embed; decision paints `webhook.editMessage(decisionViewToDiscord(view))`. The "⚔️ Action"/unsafe/invalid embeds stay inline (not view-states). `getCurrentScene` already a controller dep (M3.2).
  Scope fence: do NOT touch `action:dayjob:custom` / `action:custom:modal` (M3.5); no engine changes (`commuteToWorkplace`/`startAction` exist); byte-identical (oracle `action:dayjob:` outcome transcript + snapshots, zero churn).
[x] **M3.5 — Custom action modal.** _(done 2026-07-19, commit `3ce9eb0`)_ Ported the `action:custom:modal` submit through `SessionController.beginCustomAction`/`runCustomAction`; extracted a shared `renderStartResult` fan-out helper (`runWork` now delegates to it too). Profanity guard, the `action:dayjob:custom` modal-open, and the `stash/consumeMenuMessage` maps stay adapter (Bucket A / C.3), confirmed by recon. Fresh-context review: sound, byte-identical for BOTH the custom-modal and dayjob transcripts; one LOW consistency nit (null-guard style) applied. Zero churn.

  M3.5 design calls + checklist (lead-settled 2026-07-19). Single commit. Scope reality after recon: the only GAME-flow to port is the `action:custom:modal` SUBMIT (start-from-typed-text). The `action:dayjob:custom` modal-OPEN (`showModal` + `consumeMenuMessage` + `WebhookClient.deleteMessage`) and the menu-message maps are pure transport (Bucket A / modal round-trip protocol C.3) and STAY adapter. The submit leaf uses `deferReply`/`editReply` (not `deferUpdate`/`webhook.editMessage`), so the adapter paints with `editReply`.
  [I] **Profanity stays an adapter flow guard (DC-M1).** The adapter keeps `checkProfanity(description)` BEFORE `deferReply` (its current spot) and replies the rejection inline; the controller is only called with already-clean text. ("Stays a flow guard" = remains a guard in the flow; no need to drag `discord/profanity.js` into the controller.)
  [I] **Controller custom-action flow (DC-M2).** `beginCustomAction(userId): { kind: 'no-character' } | { kind: 'resume'; view: DecisionViewState } | { kind: 'start' }` — getCharacter null -> no-character; `char.lastActionState !== null` -> `resumeAction` (synchronous; NO stale-empty guard and NO try here — a resume throw propagates to the adapter's outer try -> "Could not act", matching the leaf) -> `{ kind:'resume', view: buildDecisionView(nextDecision, state.decisions.length, state, char) }`; else `{ kind:'start' }`. `runCustomAction(userId, description): StartRenderResult` — `prevChar = getCharacter(userId)`, `startAction(prevChar.id, description)` (NO opts — default quest kind, no wage), delegate to the shared `renderStartResult`.
  [I] **Extract the shared fan-out (DC-M3).** The `action:custom:modal` outcome/empty/decision branches are byte-identical to `runWork`'s (compact private + full public, NO `classEmoji`, "⚔️ Action" empty embed, decision via `buildDecisionView(firstDecision, 0, state, prevChar, actionType)`, NO divine-intervention branch). Extract a private `renderStartResult(userId, prevChar, result): { kind:'outcome'; viewPrivate; viewPublic; distilledType; actionId?; characterName; char; prevChar } | { kind:'empty-action'; prompt } | { kind:'decision'; view }` and refactor `runWork` to `startAction(...) -> renderStartResult(...)` too (the dayjob oracle snapshot gates that refactor). `characterName = char.name`; `char = getCharacter(userId) ?? prevChar`.
  [I] **Adapter (DC-M4).** Rewrite the `action:custom:modal` submit leaf: profanity guard (inline, pre-defer) -> `deferReply` ephemeral -> delete stale menu (`consumeMenuMessage` + `WebhookClient`) -> inside the existing outer `try/catch (notifyAdmin "Action (custom modal) failed" + editReply "Could not act" with `embeds: []`)`: `beginCustomAction` -> no-character `editReply` guard / resume `editReply(decisionViewToDiscord(view))` / start -> paint the thinking screen (inline, clipped description, exactly `**You:** ${clipped}\n\n⏳ **Thinking…**\n_${idle}_`) -> `runCustomAction` -> outcome `editReply(outcomeViewToDiscord(viewPrivate))` + nav/service + public `payload` (`**${name}** <@id> — ${distilledType}`, full) + `broadcastOutcome` + `announceCollapse(char.name, prevChar, char)` / empty-action inline "⚔️ Action" `editReply` / decision `editReply(decisionViewToDiscord(view))`. Leave `action:dayjob:custom` modal-open + the menu-message maps untouched.
  Scope fence: modal-open + `stash/consumeMenuMessage` stay adapter; no divine-intervention branch added (matches the leaf); no engine changes; byte-identical (oracle `action:custom:modal` transcript + `action:dayjob:` transcript, zero churn).
[x] **M3.6 — nav generic + nav:sleep + slash-arm flow.** _(done 2026-07-20, commit `d9e96a0`)_ Ported the two game-flow bits left in the three named leaves through `SessionController.stampLastPlayed` (DC-N, replaces the nav-click + slash-arm inline stamps) and `needsCharacterGate` (DC-O, the reroute-to-join decision; `CHARACTER_GATED_COMMANDS` moved from a `DispatchDeps` field to the controller's 4th constructor param). Recon confirmed the residue — generic `nav:` route, `nav:sleep` loading beat, slash-arm painting — is pure Bucket A transport (`navResponseMode`/`buildComponentPayload`/`getNavButtons`/`ephemeral` policy/sleep goodnight row/error funnel/registry invocation all stay adapter). Byte-identical: typecheck clean, 82 files / 1519 tests green, zero snapshot churn. Fresh-context adversarial review: byte-identity confirmed across all three call sites (short-circuit order `has → characterExists` preserved), `DispatchDeps` cleanup complete in both wirings; zero findings (two informational FYIs only — no direct unit tests on the two thin helpers, consistent with prior DC-* slices; a pre-existing prettier-not-wired note).

  M3.6 design calls + checklist (lead-settled 2026-07-20). Single commit (two small controller helpers + a constructor-dep move; no view-states). Recon reality: across the three named leaves the ONLY game-flow filling left to extract is (a) the `updateLastPlayed` stamp (nav-click ~767-768 + slash-arm ~133-137) and (b) the character-gate decision (slash-arm ~120-126). Everything else these leaves do — `registry` handler invocation, `getNavButtons`/`buildComponentPayload`/`navResponseMode`/`ephemeral` policy, the sleep goodnight button row, banner files, the loading-beat paint, the error funnel — is Bucket A transport and STAYS (scope fence). So M3.6 moves those two decisions and confirms the residue is transport-only (this is what M3.7's "dispatcher is transport-only" check verifies).
  [I] **`stampLastPlayed` (DC-N).** Add `SessionController.stampLastPlayed(userId): void` = `getCharacter(userId)` then `if (char) updateLastPlayed(char.id)`. Replaces the nav-click stamp and the slash-arm stamp (the latter still guarded by `commandName !== "join"` in the adapter). `beginDayJob`'s own inline stamp stays as-is — it already holds the char from its guard, so routing through the helper would re-fetch. Byte-identical: same two engine calls, same order.
  [I] **`needsCharacterGate` + constructor-dep move (DC-O).** Add `SessionController.needsCharacterGate(userId, commandName): boolean` = `this.characterGatedCommands.has(commandName) && !this.engine.characterExists(userId)` (membership checked first, preserving the adapter's short-circuit so `characterExists` only runs for gated commands). `CHARACTER_GATED_COMMANDS` moves from a `DispatchDeps` field to a `SessionController` constructor param (4th, `ReadonlySet<string>`, default `new Set()` so the existing 3-arg controller unit tests still compile); `index.ts` + the dispatch harness pass the real set to the controller and drop it from `DispatchDeps` (field + destructure + the doc comment listing it). The adapter keeps the registry swap (`registry.get("join")` -> `activeHandler`), driven by the boolean. Byte-identical: same set + `characterExists` check, same reroute.
  Scope fence: no view-states added; `navResponseMode`/`buildComponentPayload`/`getNavButtons`/`ephemeral` policy/sleep goodnight row/error funnel stay adapter; `join:` untouched (M3.7); no engine changes; byte-identical (oracle full transcript set + M2 snapshots, zero churn).
[x] **M3.7 — Closeout.** _(done 2026-07-20)_ Verified: no live `pendingDecisions`/`getChoiceLabel`/`setPendingDecision` refs anywhere in `src/` (only historical DC-A rationale comments in the engine remain); the dispatcher's only residual `engine.*` reads are `getCharacter` for nav-button payload assembly + the join render, and `getMeta(META_RECAP_THREAD_ID)` for broadcast routing — both Bucket A transport, no game-flow left. `join:` stays adapter-delegation as planned. Oracle + M2 snapshots green (typecheck clean, 82 files / 1519 tests, zero churn). `CHANGELOG.md` `[Unreleased] › Internal` gained the milestone-level M3 entry; M3 ticked in [[layer-boundaries-and-json-seam]].

Scope fence (whole milestone): the error funnel/guard plumbing (parent decision 5) is out of scope — it stays wrapping the adapter for [[discord-interaction-layer]] later. No `sim/` changes. Every slice keeps Discord output byte-identical (oracle + M2 snapshots, zero churn); a snapshot change means the port drifted — fix the port, never re-bless.

Execution state: _done 2026-07-20._ Branch `feat/m3-controller-extraction` off `9a0776d`. All slices green, byte-identical throughout (zero snapshot churn). M3.0 `825ae41`, M3.1 `1432d59`, M3.2 `dc7b5a1`+`a9c30d1`+`a1363c7`, M3.3 `890c297`+`c04d2b0`, M3.4 `f211a4e`, M3.5 `3ce9eb0`, M3.6 `d9e96a0`, M3.7 closeout (docs). Final: typecheck clean, 82 files / 1519 tests green. Ready to merge to `dev`; M4 (agent-player adapter) is next and reuses `SessionController`.

### M3.2 — task checklist (lead-settled 2026-07-19)

Slice goal: kill the Discord-side option-resolution state and route the `action:choice`/`action:bail` leaf through the controller, byte-identical. Split into three independently-verifiable commits (engine resolution, verbatim builder relocation, controller+adapter+deletions); the oracle (`tests/discord/dispatch-oracle.test.ts`) + M2 action snapshots gate each. Baseline before start: typecheck clean, 81 files / 1512 tests green.

Design calls (lead, within decisions 1 + 2 and the M3.1 seam):

[I] **Engine owns resolution; `stepAction` stays label-based (DC-A).** Add `resolvePendingChoice(characterId: number, selector: PendingChoiceSelector): string | null` to `WorldEngine` + `WorldEngineImpl` + `MockWorldEngine`, where `PendingChoiceSelector = { kind: 'option'; index: number } | { kind: 'bail' }`. It reads `last_action_state.pendingDecision.options` and replicates the deleted Discord logic exactly, so behaviour is unchanged: no `last_action_state` present, bail returns `'Bail'` and option returns `null`; otherwise normalise empty options to the synthetic `[{ label: 'Continue', dcModifier: 0 }]` (mirroring the old `setPendingDecision`), then bail returns the first `dcModifier === null` label `?? 'Bail'`, option returns `normalised[index]?.label ?? null`. `stepAction`'s signature and `machine.step`'s label matching are untouched: the resolved label is echoed by the adapter and passed straight into `stepAction`. Accepted, oracle-uncovered nuance (flag at review, do not contort the engine to preserve): clicking a stale decision button after its action already resolved (`last_action_state === null`) now paints "session expired" via the option path instead of the old map's stale-label-then-"Action Failed"; the engine-owned read is the more honest of the two, same spirit as M0's fresher re-read.

[I] **Relocate the two view-builders to a `discord.js`-free module (DC-B).** Verbatim-move `buildDecisionView`, `buildOutcomeView`, and the helpers they exclusively use (`quoteLines`, `dcArrow`, `statEmoji`, `buildStoryThread`, `chooseContinueBorder`, `renderCombatStatusFrame`, `renderCombatStatus`, `shortLabel`, `outcomeColorIntent`, `capitalize`, `INSIGHT_MARGIN`, the `LETTERS` array) plus the choice/bail custom-ID vocabulary the builder embeds (`CID_PREFIX`, `CID_BAIL`, `choiceCid`, `parseActionCid`) out of `src/discord/commands/action.ts` into a new `src/view/actionViewState.ts` that imports nothing from `discord.js` (only `render/*`, `engine/*`, `view/viewState`, and `discord/format` for `dayJobEmoji`, all `discord.js`-free). `action.ts` re-imports these and keeps the medium wrappers `buildDecisionMessage`/`buildOutcomeEmbed` (they weld `discord.js`) plus `withNarration` (adapter-only, used by the stale-embed). Any helper also referenced by surviving `action.ts` code stays exported and is re-imported; the `CID_DAYJOB*`/`CID_CUSTOM*` constants stay in `action.ts` (adapter-only). Same mechanical, byte-identical, zero-churn move as M3.0; confirm the new module has zero `discord.js` transitive imports.

[I] **Controller emits ViewState + semantic outcome facts (DC-C, revised post-review).** Extend the constructor to `new SessionController(engine, getCurrentScene)` (`getCurrentScene` is `discord.js`-free). Split resolution so the adapter can preserve the old ack ordering (see the DC-D fix): `beginChoice(userId): { kind: 'no-character' } | { kind: 'ok'; character: CharacterData }` does ONLY the `getCharacter` guard; `resolveChoice(character, selector): string | null` wraps `engine.resolvePendingChoice(character.id, selector)`. Add `async stepChoice(userId, label, prevChar): Promise<StepChoiceResult>` where `StepChoiceResult = { kind: 'decision'; view: DecisionViewState } | { kind: 'outcome'; view: OutcomeViewState; distilledType: string; actionId?: number; characterName: string; characterClass?: string | null; char: CharacterData | null; prevChar: CharacterData }`. `stepChoice` calls `engine.stepAction(prevChar.id, label)` and does NOT catch (it lets stepAction/view-build errors propagate, so the adapter's inner catch is the single error boundary, matching the old `handleActionChoice` inner try that wrapped stepAction + applyActionResult together); on resolved it re-reads `getCharacter(userId)`, builds the `OutcomeViewState` via `buildOutcomeView(outcome, char, getCurrentScene(userId), state, undefined, engine)` (NOT compact, matching the old `applyActionResult`) and returns the outcome facts; on non-resolved it builds `DecisionViewState` via `buildDecisionView(nextDecision, state.decisions.length, state, char ?? undefined)` (no `actionType`/enemy args on a CONTINUE beat). Imports `buildDecisionView`/`buildOutcomeView` from `src/view/actionViewState.ts`; never imports `discord.js`.

[I] **Adapter rewrite keeps all transport + payload + broadcast (DC-D, revised post-review).** Rewrite the `action:` leaf (`dispatchInteraction.ts` around line 761) byte-identical to the old `handleActionChoice` + `applyActionResult`, reproducing the exact ack ORDER (the M3.2c review caught two drifts in the first cut): `beginChoice(userId)`; `no-character` replies ephemeral BEFORE any defer; then `await deferUpdate()` UNCONDITIONALLY (the old defer was before the customId parse); THEN build the selector (`CID_BAIL` -> bail, else `parseActionCid` -> option) and `if (!parsed) return` AFTER the defer (so a malformed `action:` id is still acked, matching old); `resolveChoice` null -> "session expired" `webhook.editMessage`; paint the inline "**You:** {label}\n\n⏳ **Thinking…**\n_{idle}_" embed (built inline, unchanged, since `LoadingViewState` is M3.4). Then an INNER `try` wraps `stepChoice` + the paint + `broadcastOutcome` + `announceCollapse` together, with a `catch` that reproduces the old inner catch exactly (`console.error('[action] stepAction error:', err)` + a "⚔️ Action Failed" `webhook.editMessage`) — so a broadcast/announce failure repaints "Action Failed" as before, NOT the outer funnel. `decision` paints `webhook.editMessage(decisionViewToDiscord(view))`; `outcome` paints `webhook.editMessage` with `outcomeViewToDiscord(view)` + `getNavButtons`/`getOutcomeServiceButtons`, assembles the public `payload` (content line via `classEmoji(step.characterClass)`, `getPublicOutcomeButtons`), `broadcastOutcome`, then `announceCollapse(step.char?.name ?? step.prevChar?.name ?? 'A soul', step.prevChar, step.char)` (use the raw `char?.name` chain, matching old exactly). The outer error funnel (`notifyAdmin` + generic reply) stays wrapping the whole leaf (Bucket A / decision 5). Accepted micro-divergence (debug-only, not gated): the VERBOSE "action: done" log now fires on the decision/outcome/error paths but not the early no-character/expired/parse-null returns.

[I] **Delete the dead state (DC-E).** Remove from `action.ts`: the `pendingDecisions` map, `setPendingDecision`, `getChoiceLabel`, `handleActionChoice`, `applyActionResult`. Remove every now-dead `setPendingDecision(...)` call across `dispatchInteraction.ts` (nav:action resume, custom-modal, dayjob leaves) and `action.ts` (`makeActionCommand`), keeping the `buildDecisionMessage` render that followed each. Clean the resulting unused imports (typecheck-guided). This is the point at which the map has zero readers, so all writes are safe to drop even in leaves not yet ported.

[I] **Rewire the oracle to seed the engine, not the map (DC-F).** The two `action:choice` transcripts seed resolution via the deleted `setPendingDecision(userId, ...)`; replace with seeding `MockWorldEngine` so `resolvePendingChoice` returns `DECISION_RESULT.firstDecision.options[0].label` (`"Advance carefully"`) for `{ option, 0 }`. Add a mock setter (e.g. `setPendingChoiceOptions(options)`) whose `resolvePendingChoice` mirrors the real normalise/bail/index contract. Update the harness to `new SessionController(engine, getCurrentScene)`. The oracle must keep asserting `stepAction[0].choice === "Advance carefully"` and produce zero snapshot churn.

M3.2a — Engine option-resolution (`dc7b5a1`):
[x] `PendingChoiceSelector` + `resolvePendingChoice` added to `WorldEngine`/`WorldEngineImpl`/`MockWorldEngine`. No caller yet: behaviour unchanged.
[x] `tests/engine/resolve-pending-choice.test.ts` covers option-index, bail-find, empty-options Continue fallback, no-state, out-of-range. Verified: typecheck clean, 82/1517 green, zero churn.

M3.2b — Relocate view-builders (`a9c30d1`, verbatim):
[x] `src/view/actionViewState.ts` created; builders + exclusive helpers + choice/bail cid vocabulary moved verbatim; `action.ts` re-imports; medium wrappers + `withNarration` stay. Verified `discord.js`-free (direct + transitive via madge), typecheck clean, 82/1517 green, zero churn.

M3.2c — Controller + adapter + deletions + oracle (`a1363c7`):
[x] Controller `beginChoice`/`resolveChoice`/`stepChoice` + `getCurrentScene` constructor dep, wired at `index.ts` + harness. Adapter leaf rewritten (DC-D); dead state + dead `setPendingDecision` writes + unused imports deleted; oracle reseeded via `setPendingChoiceOptions`.
[x] Fresh-context adversarial review found two real byte-drifts (broadcast/announce error boundary; deferUpdate-before-parse ordering) + a tautological test; all fixed and locked by two new dispatch regression tests + a restored decoration-vs-raw-label test. Verified: typecheck clean, 82/1519 green, zero churn, no live `pendingDecisions`/`getChoiceLabel`/`setPendingDecision` refs in `src/`.

Scope fence (M3.2): do not port `nav:action`, `action:dayjob:`, or `action:custom:modal` (later slices) beyond deleting their dead `setPendingDecision` write; do not add new view-states (`LoadingViewState`/`MenuViewState` are later slices) so the interim "Thinking…" screen stays an inline adapter embed; do not touch the error funnel/guard (decision 5); no `sim/` changes; `stepAction`/`machine.step` signatures unchanged; every commit keeps Discord output byte-identical.

## M4 — Agent-player adapter

Goal: an in-process, Discord-free "agent-player" — a peer adapter over the M3 seam — that stands up a real `WorldEngine` + `SessionController` and plays the game with a real LLM brain, all features on. It is the "whole game, shorter horizon" harness (decision 6): distinct from `sim/` (engine-level, LLM-cut, months-of-data) — the agent-player enters at the controller with the real LLM for **bug-hunting/QA and playtest feedback**. Both are M4 deliverables (scoping call below).

### Scoping calls (lead + user, 2026-07-20)

[I] **Goal = QA + feedback (both).** The harness must (a) surface crashes/dead-ends/broken flows as a failure log + repro transcript (QA), and (b) produce qualitative playtest feedback (pacing, clarity, fun) as a report. Sliced so QA lands before feedback (a run that can't complete has nothing to critique).
[I] **Seam = direct controller calls (decision 3, in-process).** The agent calls `SessionController`'s existing typed methods in-process — the M3 methods **are** the seam today. No formal JSON event-router, and the Discord adapter is **not** rebuilt onto a shared protocol (that stays a later, optional bolt-on; the gap table's "protocol seam" is deferred, not dropped). The just-cleaned dispatcher is not re-touched.
[I] **Brain = real LLM from the first playing slice.** The agent's move-picker is a real DeepSeek call (this repo's LLM is DeepSeek via `callDeepseek`, not Anthropic). Tests + CI use a deterministic scripted stub (no network); the real gateway is the default only on an explicit harness run (needs `DEEPSEEK_API_KEY`). Mirrors the existing `ProdPipelineLlmGateway` (real) / `PipelineScriptedGateway` (stub) split exactly.

### Recon findings (2026-07-20) — the construction recipe

[I] **Engine = prod-faithful `WorldEngineImpl`, not `PipelineSimEngine` (DA-1).** "All features on" needs the day/tick economy (roll refill, day-job income, stamina/health regen) — `PipelineSimEngine` deliberately never ticks, so it can't play across days. So the harness builds a `WorldEngineImpl` the way `sim/engine-factory.ts:110-135` does but with the pieces sim omits: `initDb(':memory:')` + `migrate` (seeds `SEEDED_LOCATIONS`/`SEEDED_EDGES`), the five repos, **real** assets via `loadCharCreationAssets()` + `buildDayJobIncomeMap()` (`index.ts:172-189`) so `createCharacter` derives real stats, and a **real** `ProdPipelineLlmGateway({ apiKey, model })` as `pipelineLlmGateway`. Nightly advance via `advanceDays(engine, 1)` (`sim/time.ts`).
[I] **The brain is net-new but every block exists (DA-2).** No "LLM picks a move from a rendered screen" helper exists — `decide`/`classify` *author* menus, they don't *consume* them. Build it by mirroring the pipeline-gateway pattern: reuse `callDeepseek` (`src/llm/deepseek-transport.ts:44`) verbatim (audit-in-`finally`, throw-loud, JSON-mode), a versioned prompt under `assets/prompts/agent-player/` + an `AGENT_PLAYER_VERSION` constant (per the `prompt-versioning` skill), and a scripted stub modelled on `PipelineScriptedGateway`.
[I] **No text renderer exists, but none is missing (DA-3).** Every `ViewState` field is already a `string`; `viewToDiscord` just joins them. A pure `viewToText(view): string` peer (agent medium step) is a mechanical join — realises decision 2 ("agent sees close to what a Discord player sees") without touching the ANSI `render/*` frames (already baked into the view's string fields).
[I] **Three bookends are Discord-only (DA-4).** The mid-day action loop is fully controller-reachable, but **character creation** (`join` wizard), **rest + nightly tick** (`engine.restAtOak`/`engine.tick(true)`), and **`/hi`** have no controller seam and no `ViewState`. The agent calls the **engine directly** for these bookends (matching what `sim/` does); the interesting per-action loop goes through the controller. Extending the controller to own the bookends is explicitly **out of M4 scope** (would re-open the M3 surface) — revisit only if a later frontend needs it.

### Settled design calls

[I] **Home = `src/agent/` (DA-5).** A new top-level adapter dir, peer to `src/discord/` and `src/sim/` — never imported by either. An `npm run agent:play` script is the entry point; the real-LLM run is opt-in (manual), never wired into `npm test`.
[I] **`AgentMove` shape (DA-6).** The brain returns a discriminated union feeding the controller/engine directly: `{ kind: 'menu-pick'; index }` (a day-job menu button) | `{ kind: 'custom'; text }` (free-text action) | `{ kind: 'choice'; index }` / `{ kind: 'bail' }` (a decision button → a `PendingChoiceSelector`) | `{ kind: 'sleep' }` (end the day). The harness maps each to the right controller/engine call; illegal moves for the current screen are re-prompted or logged as a QA finding.

### Slice sequence

Order is binding. M4.0–M4.1 are pure/unit (no network, no engine wiring); M4.2 first stands the harness up; QA (M4.4) precedes feedback (M4.5).

[x] **M4.0 — Agent text view (`viewToText`).** _(done 2026-07-20)_ Pure `src/agent/viewToText.ts` (home = `src/agent/` per DA-5, not `src/view/`; imports only view-state types, no `discord.js`): `viewToText(view): string` maps every `ViewState` variant to agent-readable text — the full (uncollapsed) variant always, no embed-length degradation ladder (an LLM context has no 4096 cap) — and `viewMoves(view): ViewMove[]` enumerates the discrete actionable buttons positionally. `ViewMove` is deliberately narrower than the eventual `AgentMove` (DA-6): it is view-derived button data (`choice`/`bail`/`menu` + the `favoured` insight hint), while free-text/sleep moves are contextual, harness-supplied. Fresh-context review (sound, no blocking defects) surfaced one parity gap fixed here — the `favoured` hint (a green button to a Discord player) now rides `ViewMove` + a `(favoured)` text marker (decision 2) — plus three added coverage cases (bail-only decision, empty menu, minimal outcome). Typecheck clean, 83 files / 1534 tests green (+15).
[x] **M4.1 — `AgentPlayerGateway` seam.** _(done 2026-07-20)_ Defined the `AgentPlayerGateway` interface (`chooseMove(input): Promise<AgentMove>`) + `AgentMove` (DA-6, five variants) + input shape (`ChooseMoveInput` = `viewToText` screen + `LegalMove[]` + brief `AgentCharView`) in `src/agent/`; implemented `ProdAgentPlayerGateway` (reuses `callDeepseek` verbatim — JSON mode, single attempt, throw-loud, records one `llm_calls` row in `finally`; mirrors `ProdPipelineGateway`) and `ScriptedAgentPlayerGateway` (deterministic move-list playback, fails loud on exhaustion). Versioned prompt `assets/prompts/agent-player/agent-v1.md` + `current_source.md` mirror; `AGENT_PLAYER_VERSION` + `loadAgentPrompt` in `src/agent/agentPrompt.ts` (deliberate DA-5 deviation from the skill's "constant in prompt-builder.ts" — the QA adapter owns its family so core LLM code stays free of it; every prompt-versioning *rule* honoured, rows stamped `agent-v1` / callKind `agent-player`). The brain picks by list-position index (LLM contract `{ thought, choice, text? }`), the gateway maps back to the stored `AgentMove` so `menu-pick`/`choice` carry the VIEW button index not the list position. Unit tests (`tests/agent/agent-gateway.test.ts`, 15) cover stub playback + exhaustion, request assembly, parse (out-of-range/non-integer/empty-custom/non-2xx/unparseable all throw loud), the DA-6 index-preservation crux, and audit recording (success + diagnostic rows). No harness run yet; no real network in `npm test`. Typecheck clean, 84 files / 1548 tests green (+14). Fresh-context adversarial review: sound, no blocking issues — one coverage gap fixed (the DA-6 divergent-index test); three informational notes (deep-capture diagnostic-only is intentional for M4.1; `Number(choice)` coercion matches repo convention; no automated `current_source` byte-guard is a pre-existing repo-wide gap).
[x] **M4.2 — Harness bring-up + single action end-to-end.** _(done 2026-07-20)_ `src/agent/engineHarness.ts` `buildAgentEngine` builds the Discord-free `WorldEngineImpl` (DA-1 recipe: `initDb(':memory:')`+`migrate`, five repos, real char-creation assets → real stat derivation, `dayJobIncome`, real scenes via `SceneLoader`+`TagResolver` → a faithful `getCurrentScene`; a pluggable pipeline gateway — injected scripted for tests, real `ProdPipelineLlmGateway` from `apiKey` on a live run). `src/agent/harness.ts` `AgentHarness` seeds the character engine-direct (DA-4 bookend) and drives one action through the controller with the brain: `stampLastPlayed` → `openActionMenu` → `viewToText`/`viewMoves` → `chooseMove` → day-job (`beginDayJob`/`commuteForWork`/`runWork`) or custom (`beginCustomAction`/`runCustomAction`) → the decision loop (`beginChoice`/`resolveChoice`/`stepChoice`) until outcome, emitting a structured `Transcript` (`src/agent/transcript.ts`). `src/agent/agentMoves.ts` maps view buttons → `LegalMove[]` authoritatively via customId (`parseActionCid` for the option index, `CID_DAYJOB*` for menu picks) plus the contextual `sleep`/`custom` moves, and `isLegal` guards illegal picks. Tests (`tests/agent/harness.test.ts`, 4) drive custom + day-job + immediate-outcome + sleep, fully deterministic (scripted brain + `PipelineScriptedGateway` + fixed d20, no network). Typecheck clean, 85 files / 1553 tests green. Fresh-context adversarial review: seam usage faithful; one CONFIRMED bug fixed + locked (the immediate-outcome path transcripted `viewPublic`, the broadcast copy, instead of `viewPrivate` — the acting player's compact screen, decision 2); `stampLastPlayed` added to match the Discord `nav:action` DC-N stamp (avoids spurious absence-nudge findings on multi-day runs); commute-beat transcript event + broader guard-arm coverage deferred to M4.4 (its explicit remit); asset-loader duplication confirmed necessary (`index.ts` self-executes; loaders unexported) and byte-identical.
[x] **M4.3 — Full-day + multi-day loop.** _(done 2026-07-21)_ `AgentHarness.playDay()` loops `playOneAction` to a day-end disposition — a completed action returns to the menu; `slept`/`no-rolls` end the day cleanly; `no-character` is fatal; a run of `STUCK_LIMIT` (5) consecutive non-outcome stumbles (dead-end/illegal/abandoned, reset by any outcome) or a `MAX_ACTIONS_PER_DAY` (50) backstop ends it `stalled` with a logged finding — returning a `DaySummary { dayNumber, outcomes, ended }`. `playDays(n)` bookends each non-fatal day with the engine-direct DA-4 rest+nightly-tick (`restAtOak` + `tick(true)` — admin bypasses the wall-clock cron-idempotency guard so days deterministically advance and rolls refill; `dayNumber` read via `getMeta('day_number')`, the same source `tick` writes) and records a `day` boundary; it stops early on `no-character` OR `stalled` (a stalled day leaves its wedge in place and the auto-expiry gates on real wall-clock, so pressing on would replay the identical frozen state every remaining day — burning a real LLM run with no fresh signal). `src/agent/play.ts` + the `agent:play` npm script are the DA-5 opt-in real-LLM entry (gated on `DEEPSEEK_API_KEY`: real pipeline + real `ProdAgentPlayerGateway` brain, both DeepSeek; seeds a character, `playDays(AGENT_DAYS)`, prints the transcript). Tests (`tests/agent/harness.test.ts`, +3): a no-rolls day (3 goblin actions deplete the allowance; the 4th menu short-circuits before the brain — `brain.calls === 9` proves it), a 2-day sleep run (day advances 1→2→3, spent roll refilled via the night, guard rested back at the Oak), and a stall stopping the run (5 illegals → one `stalled` summary, no 2nd day, no tick). Fully deterministic (scripted brain + scripted pipeline + fixed d20, no network). Typecheck clean, 85 files / 1556 tests green (+3). Fresh-context adversarial review: happy-path loop, DA-4 sequencing and scope-fence sound; two confirmed defects fixed + locked (the stalled-day replay-across-days silent burn → `playDays` now stops on `stalled`; a Saturday-UTC-fragile roll-refill assertion → asserts `>=` the seeded allowance since `tick`'s Saturday bonus rides the real clock) plus a `NaN` `AGENT_DAYS` guard on `play.ts`. One fidelity gap deferred to M4.4 (below) by decision.
[x] **M4.4 — QA capture (goal a).** _(done 2026-07-21)_ Turned the harness into a QA instrument. Every controller/engine/brain call in the action path runs through `seam(phase, fn)`, which leaves a `this.phase` breadcrumb; `playOneAction` try/catches so any uncaught throw becomes an `error` finding naming the failing call + a new `crashed` `PlayResult` disposition — the run ends (`DaySummary.ended: 'crashed'`, `playDays` stops) but the transcript survives as the repro, and `play.ts` dumps it in a `finally` so even a throw mid-run writes what it saw. `checkInvariants(phase)` runs after every outcome and every nightly tick (health/stamina in range, `wealth >= 0`, `rollsRemaining >= 0` → non-fatal `error` findings) and is itself self-guarding (it's called outside `playOneAction`'s catch, so a throwing `getCharacter` there becomes a finding, not an escape). `endDay` is now faithful to real play (M4.4 sleep-fidelity decision — the user chose "rest only if rolls spent"): `restAtOak` runs ONLY when `rollsRemaining === 0` (an idler with rolls left keeps their location), the tick runs regardless, read before the tick refills; it returns false (naming the failing step) on a bookend throw so `playDays` stops. The M4.2 deferrals are folded in: a `commute` transcript beat in `doDayJob` (rendered via `viewToText`, deterministic idle) and broadened arm coverage. `Transcript.summary()` rolls the log into a run scoreboard (turns/outcomes/dead-ends/commutes/day-boundaries/findings). Tests (`tests/agent/harness.test.ts`, +9 → 16 total): stub-`SessionController`/`WorldEngine`-driven arms (crash mid-action + standalone, invariant breach, throwing tick, `openActionMenu` resume-*/no-character, `beginDayJob` invalid-job/unsafe) + real-engine faithful-`endDay` both branches (idle keeps location, spent rolls rest home) + the run summary. Typecheck clean, 85 files / 1565 tests green. Fresh-context adversarial review: sound; one CONFIRMED gap fixed (the post-outcome `checkInvariants` escaped the crash net — now self-guarding + `play.ts` dumps the transcript in `finally`) plus the `endDay` per-step breadcrumb, the commute count, and the mid-action-crash test; the unsafe-rest HP penalty gap flagged for M4.5 (below).
[x] **M4.5 — Feedback pass (goal b).** _(done 2026-07-21)_ Built a playtest critic as a SECOND agent-player prompt family (the in-game per-beat `critic/` family — `CritiquedLlmGateway` gating a single decision — doesn't fit a whole-run report, so not reused). `PlaytestCriticGateway.critique(input)` seam (`CritiqueInput` = the completed transcript `events` + `summary()`; `PlaytestReport` = `{ pacing, clarity, fun, difficulty, summary }`, prose per dimension) with `ProdPlaytestCriticGateway` (real DeepSeek — mirrors `ProdAgentPlayerGateway` exactly: `callDeepseek` verbatim, JSON mode, throw-loud, one `llm_calls` row recorded in `finally`, `callKind: 'agent-critic'`; `buildCritiqueMessage` renders `RUN SUMMARY` + a one-line-per-event `PLAY LOG`; `resolveReport` validates all five non-empty string fields; temperature 0.4, below the brain's 0.7 — analysis, not roleplay) and `ScriptedPlaytestCriticGateway` (deterministic stub). Versioned prompt `assets/prompts/agent-critic/agent-critic-v1.md` + `current_source.md` mirror; `AGENT_CRITIC_VERSION`/`AGENT_CRITIC_STAMP` (`agent-critic-v1`, distinct from the in-game `critic-v1`) + `loadCriticPrompt` in `src/agent/criticPrompt.ts` (same DA-5 divergence as the brain — the adapter owns its prompt families). The prompt bakes in BOTH fidelity caveats below so the critic discounts them. `play.ts` runs the critic after a completed run (opt-in, in its own try/catch so a critic failure never buries the printed run output). Tests (`tests/agent/critic-gateway.test.ts`, +11): scripted stub, `buildCritiqueMessage` render of every event kind, Prod parse/throw-loud (missing/blank field, non-2xx, unparseable) + audit rows — all via mocked fetch, no network. Typecheck clean, 86 files / 1576 tests green. Fresh-context review: sound, no blocking issues; three nits fixed (a `dead-end` render-coverage gap in the sample run, the versioned file renamed `critic-v1.md` → `agent-critic-v1.md` to not collide with the in-game family's basename, and a more realistic sample offer/choice); an unbounded-transcript-vs-context-window risk noted only if `AGENT_DAYS` is bumped well past 1.

  **Two fidelity caveats fed to the critic (so it discounts harness-only artefacts):** (1) the agent may voluntarily `sleep` with rolls unspent (a real player can't `/sleep` then, though they can idle) — the M4.4 `endDay` models the *rest* faithfully (only when rolls spent) but the *early day-end itself* is a harness affordance; (2) the harness's `endDay` uses `engine.restAtOak` directly, which does NOT apply the `/sleep` command's unsafe-rest −1 HP penalty (that lives in `src/discord/commands/sleep.ts`, not the engine), so the harness never surfaces unsafe-rest-penalty feedback.
[x] **M4.6 — Closeout.** _(done 2026-07-21)_ Verified the isolation both ways — `grep` finds no import of `src/agent/*` under `src/discord/` or `src/sim/`, and `src/agent/` imports nothing from either (agent TESTS import `src/sim/PipelineScriptedGateway`, the deterministic pipeline stub — test-only, does not violate SOURCE isolation). `npm test` is stub-only: both `ProdAgentPlayerGateway` and `ProdPlaytestCriticGateway` are constructed in tests with an injected `fetch`, so no test touches the network; the real DeepSeek path runs ONLY on the opt-in `npm run agent:play`. Real-LLM smoke run documented below. `CHANGELOG.md` `[Unreleased]` gains the M4 Internal entry; M4 ticked in the parent spec. Final: typecheck clean, 86 files / 1576 tests green.

### Running a real smoke run

The agent-player's real-LLM path is opt-in and never runs in CI. To smoke it end-to-end against live DeepSeek:

```
DEEPSEEK_API_KEY=<key> npm run agent:play                          # 1 day (default)
DEEPSEEK_API_KEY=<key> AGENT_DAYS=2 AGENT_OUT=run.json npm run agent:play   # 2 days, transcript → run.json
```

Optional env: `DEEPSEEK_MODEL` (override the model), `AGENT_DAYS` (positive integer, default 1), `AGENT_OUT` (transcript file path; default a timestamped file under the OS temp dir). It seeds a fixed Warrior/Town-Guard character, plays N days with a real DeepSeek brain over a prod-faithful `:memory:` engine (all features on), then runs the playtest critic over the completed transcript. The **transcript** (the repro) is written to the `AGENT_OUT` file as clean JSON — NOT stdout, because the engine, the verbose gateways, npm, and migrations all log to stdout during a run, so a redirect would co-mingle noise into the JSON (a defect the first smoke runs caught). **stderr** carries the transcript path, the day summaries, the run scoreboard, and the critic's report. Expect it to cost real tokens (every brain move + pipeline stage + the critique is a DeepSeek call, all recorded to the in-memory `llm_calls` table). A run that throws still writes the transcript-so-far (goal a). Keep `AGENT_DAYS` small — the critic feeds the whole transcript to one call, so a long multi-day run can approach the context window.

Scope fence (whole milestone): no changes to `src/discord/` beyond nothing (the adapter is untouched); no new controller methods for the bookends (DA-4); no formal JSON protocol/event-router (scoping call 2); `sim/` unchanged; the real LLM never runs in `npm test`. Engine/controller changes only if a genuine seam gap blocks a slice — flag it as a decision, don't smear flow back into the harness.

Execution state: _done 2026-07-21._ Branch `feat/m4-agent-player` off `dev` (`bd8db8b`). All slices green, review cadence held throughout (each slice: build → fresh-context adversarial review → act on findings → commit). M4.0 `6d03890`+`b865fec`, M4.1 `a2be63a`, M4.2 `cce05ca`, M4.3 `2f42c72`, M4.4 `2d24f70`, M4.5 `384309e`, M4.6 closeout (docs). No engine/controller/discord/sim source changed — the adapter reused the M3 seam as-is (the fence held; zero seam gaps hit). Final: typecheck clean, 86 files / 1576 tests green (+57 over the M3 baseline of 1519). Ready to merge to `dev`.

---

Parent spec: [[layer-boundaries-and-json-seam]] — milestone checkboxes and all design decisions live there; changing a decision needs a `decisions/` record.
