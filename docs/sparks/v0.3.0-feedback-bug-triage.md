---
title: v0.3.0 feedback & bug triage — dev DB scrape
status: spark
domain: spark
phase: mvp+
tags: [feedback, bugs, combat, polish, triage]
created: 2026-07-11
related:
  - poc-plus-0.3.1-polish-plan
---

# v0.3.0 feedback & bug triage (dev DB)

Scraped from `data/warden.db` on 2026-07-11. All 16 entries are from today, one character (**trombosis**, discord `395325113953943568`), all v0.3.0. Branch: `poc-plus/0.3.1-polish` (last commit `55cb3d8`).

---

## Data snapshot

### Feedback (13 rows)

| id | time | act | text |
| ---- | ------ | ----- | ------ |
| 9 | 06:06 | – | fast travel should be a feature |
| 10 | 06:06 | – | Opening art says "unknown foe" even though /look knows it's the blighted stag |
| 11 | 06:07 | – | Decision 2: can't see dice rolls — why did I lose −5 HP? |
| 12 | 06:09 | – | Decision 3 lethal blow — no art or HP trackers shown. Was at 10 HP, how lethal? No dice results |
| 13 | 06:10 | 36 | Combat bail outcome looks wrong. Wants play-by-play combat decisions on outcome. Show opening art instead of location on outcome |
| 14 | 06:11 | – | Annoying to press journal just to get look/backpack. Show all character buttons on hi |
| 15 | 06:14 | – | Add spacing between journal entries, improve headers with separators |
| 16 | 06:16 | – | Add race/alignment/class/day-job emojis to new-player welcome screen |
| 17 | 06:17 | – | Decision 2 vs stag shows "minion" on HP tracker. No dice roll visibility. Why bloodied? |
| 18 | 06:22 | – | Combat outcome art: `2/10` touches right border. Needs one char padding |
| 19 | 06:24 | 39 | Combat classified action resolved like normal skill action. Title says "combat" but doesn't follow usual combat mechanics |
| 20 | 06:27 | – | Remove "or type /action <what you do> to continue" from unfinished action screen (not possible) |
| 21 | 06:30 | 40 | Art shows rolled 22 vs DC 15 but margin is −1 — maths doesn't add up |

### Bug reports (3 rows)

| id | time | act | text |
|----|------|-----|------|
| 15 | 06:19 | 38 | Combat outcome: d20 3+9=12 vs DC 13, but margin shows **−10 HEAVY LOSS** |
| 16 | 06:19 | 38 | Shows "Minion HP 6/14" but fighting a stag NPC with 24 HP |
| 17 | 06:32 | 41 | Shows −3 HP loss when already at 1 HP |

### Linked actions

| id | time | type | outcome | rolled | DC | fb/bug ids |
| ---- | ------ | ------ | --------- | -------- | ---- | ----------- |
| 36 | 06:09 | combat | **bailed** | – | 15 | F#13 |
| 38 | 06:17 | combat | failure | 3 | 13 | B#15, B#16 |
| 39 | 06:22 | combat | failure | 6 | 15 | F#19 |
| 40 | 06:28 | combat | failure | 15 | 15 | F#21 |
| 41 | 06:31 | combat | failure | 4 | 14 | B#17 |

All LLM calls: deepseek-v4-flash, tier 0, parse_ok, HTTP 200, no errors.

---

## Status reconciliation (2026-07-11, post ANSI-D/F)

Two commits landed after the baseline `55cb3d8`: `d7a3cb5` (ANSI-D continue-frame dice line + terminal data card) and `fb9fb74` (ANSI-F opening frame). Both are *display* work; neither touched combat correctness. **2 of 13 clusters done (P1 dice visibility, P3 journal); 11 still open.** Verdicts folded into each cluster below.

## Themed clusters (priority order)

### 🔴 P0 — Combat maths bugs (blocking: wrong data)

1. **[DONE — `cd1cd89`] Margin reads "vs DC" but is a contested roll** — B#15, F#21. The terminal card now shows the enemy's contested roll (`vs N +N = N`) instead of the misleading solo `[DC N]`. The continue card still shows `[DC N]` (that *is* the DC for the next round, unchanged). **Engine math was always correct** — the defect was display-only.
2. **[PARTIAL — `cd1cd89`] Enemy HP/name wrong** — B#16, F#17. Enemy **name** now threaded from the pipeline's `combatEnemy` hint through `ActionStartResult.combatEnemyName` → `buildDecisionMessage` → both the opening frame nameplate and the continue/terminal card. **HP still pending:** max HP is still `deriveEnemyMaxHp(baseDc)` clamped to [6, 20]; the `ENEMY_HP_MAX` cap needs raising and the real NPC HP wired through `combatEnemy` (`types.ts:65` has no HP field).

### 🟠 P1 — Dice visibility on continue screens (UX gap)

1. **[DONE — `d7a3cb5`] Round-by-round dice between decisions** — F#11, F#17. The continue frame renders `d20 N +B vs DC D` / `margin ±M BAND` off the persisted `combatRounds` (the DC label is correct here — it *is* the DC for the next round).

### 🟡 P2 — Opening frame & combat flow polish

1. **[DONE — `cd1cd89`] Opening frame says "unknown foe"** — F#10. `combatEnemyName` is now threaded from the pipeline through `ActionStartResult` to the opening frame's enemy nameplate.
2. **[OPEN] Bail outcome missing combat play-by-play** — F#13. The bail path carries no `combatBeat` on its outcome (`src/sim/PipelineSimEngine.ts:272`), so the round history isn't shown.
3. **[CONFIRMED MOOT — `cd1cd89`] Combat outcome HP text touches border** — F#18. The terminal card is now a pure data card with no HP bars (the embed's stats footer below already carries ❤️); width is enforced by `composeLine`.
4. **[OPEN] Combat that doesn't feel like combat** — F#19. Action 39 classified as combat but resolved as a skill-like action. No routing change landed.

### 🟢 P3 — General 0.3.0 UX polish

1. **[OPEN] Nav button discoverability** — F#14. Hi screen should surface all character buttons. Same root as F#5; menu-framework rework, MVP-bound.
2. **[DONE — `3114411`] Journal spacing/headers** — F#15. Closed by the F#6 declutter (sectioned Chronicle/NPCs, bold outcome tags).
3. **[OPEN] Welcome emojis** — F#16. `src/discord/commands/join.ts:204` renders race/alignment/class/day-job as plain text.
4. **[OPEN] Stale hint text** — F#20. Still present at `src/discord/commands/hi.ts:246` ("or type `action <what you do>` to continue").
5. **[OPEN — deferred] Fast travel feature request** — F#9. Design question parked in `[[per-player-map-exploration]]` §9; needs a spark, not a fix.

---

## Delegation plan

### Stage 1: combat maths fix (P0)

Scope: margin *labelling* (not calculation) and enemy identity on combat outcome cards.
Branch: `poc-plus/0.3.1-polish`

[x] Margin: engine calc is correct (contested roll, `combat-dc.ts:142`). The bug is the "vs DC" label on the `d7a3cb5` dice line (`CombatCardRenderer.ts:90`, `action.ts:556`) — relabel or show the enemy's d20 so the margin reconstructs. Display-only.
[x] Enemy HP/name: sourced in `PipelineActionStateMachine.ts:455-467` — max HP is DC-derived (clamped [6,20]), name defaults to `'Minion'`. Needs the real NPC HP/name carried on `combatEnemy` (`types.ts:65` has no HP field) and the `ENEMY_HP_MAX` cap raised.

### Stage 2: continue-screen dice visibility (P1)

Scope: render per-round dice on the between-decisions continue embed.
[?] The round data is persisted (ANSI-D, commit `79d48a3`) — is it just not wired into the continue embed builder?

### Stage 3: opening frame + combat flow (P2)

Scope: fix "unknown foe" to read NPC name, add combat history to bail outcome, fix HP text padding, investigate F#19 (combat→skill downgrade).

### Stage 4: general UX polish (P3)

Scope: nav buttons on hi, journal spacing, welcome emojis, stale hint removal. Fast travel is a design question, not a bug — needs a spark before implementing.
